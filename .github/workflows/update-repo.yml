name: Legacy repository publication (disabled)

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Ro-Repo V1 is frozen and cannot publish packages."
          echo "Use the manifest-pinned Ro-Repo V2 acceptance workflow."
          exit 1
